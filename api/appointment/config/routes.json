{
  "routes": [
    {
      "method": "GET",
      "path": "/appointments",
      "handler": "Appointment.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/appointments/count",
      "handler": "Appointment.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/appointments/:id",
      "handler": "Appointment.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/appointments",
      "handler": "Appointment.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/appointments/:id",
      "handler": "Appointment.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/appointments/:id",
      "handler": "Appointment.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
